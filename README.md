# CAP CDS LSP and Syntax Highlighting for Zed

This extension uses the `@sap/cds-lsp` Language Server to provide language support for CAP CDS in Zed.

## Installation

Before installing this extension, ensure you have Node.js and `@sap/cds-lsp` installed globally:

```bash
npm install -g @sap/cds-lsp
```
